#!/bin/sh

PATH_add ./node_modules/.bin

export WORKER_NAME=`jq -r ".name" ./wrangler.jsonc`

dotenv

export BASE_WORKER_URL="$WORKER_NAME.$WORKER_SUBDOMAIN.workers.dev"
export WORKER_URL="https://$BASE_WORKER_URL"